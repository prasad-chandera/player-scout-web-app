// Undervalued feature — the Moneyball list (value gap = expected value − price).
// The server page reads the mock directly and renders ValueScatter.
export * from "./models";
export { ValueScatter } from "./components/ValueScatter";
export type { ValueScatterProps } from "./components/ValueScatter";
